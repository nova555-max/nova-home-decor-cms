# Supabase (project: zfsoeketfjnnpirglosq — where admin_users live)
NEXT_PUBLIC_SUPABASE_URL=https://zfsoeketfjnnpirglosq.supabase.co
# Prefer legacy anon JWT for Auth on Netlify
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inpmc29la2V0ZmpubnBpcmdsb3NxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODM5NjI1NjAsImV4cCI6MjA5OTUzODU2MH0.1bpzOD3aOGz88h-C_pPtUopWPcMYW3b2OxR2t-ioc40

# App — set to your real Netlify URL in production
NEXT_PUBLIC_APP_URL=https://nova-home-decor.com
NEXT_PUBLIC_DEFAULT_LOCALE=ku

# Super Admin
SUPER_ADMIN_EMAIL=novahome756@gmail.com

# Server-only — sb_secret_… or legacy service_role JWT (same project)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-or-sb-secret-key

# Resend
RESEND_API_KEY=re_your_resend_api_key
RESEND_FROM_EMAIL=Nova Home Decor <onboarding@resend.dev>

# Gemini (optional)
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-3.5-flash
GEMINI_FALLBACK_MODEL=gemini-3-flash-preview

# Never enable on Netlify
# DEV_AUTH_ENABLED=false

# Password-reset OTP hashing pepper (falls back to SUPABASE_SERVICE_ROLE_KEY if unset)
OTP_PEPPER=replace-with-long-random-string

# Full /api/health diagnostics (required — service role is NOT accepted)
HEALTH_CHECK_SECRET=replace-with-long-random-string

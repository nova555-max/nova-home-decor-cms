/** Host-agnostic copy for production env instructions (Netlify primary). */
export const HOST_ENV_HINT =
  "Netlify → Site configuration → Environment variables (scopes: Builds + Functions). Locally use .env.local.";

export const HOST_SECRET_HINT =
  "Add it in Netlify → Site configuration → Environment variables (Builds + Functions), then trigger a new deploy.";
